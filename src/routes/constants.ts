export enum Pages {
  PROJECTS = 'PROJECTS',
  WALLETS = 'WALLETS',
  AUTH = 'AUTH',
}

export const paths = {
  [Pages.PROJECTS]: '/projects',
  [Pages.WALLETS]: '/wallets',
  [Pages.AUTH]: '/auth',
};
